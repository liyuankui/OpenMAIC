{
  "name": "openmaic",
  "version": "0.2.1",
  "private": true,
  "scripts": {
    "postinstall": "cd packages/mathml2omml && npm run build && cd ../pptxgenjs && npm run build",
    "dev": "next dev",
    "build": "next build",
    "build:export": "BUILD_MODE=export next build",
    "build:standalone": "BUILD_MODE=standalone next build",
    "start": "next start",
    "lint": "eslint",
    "check": "prettier . --check",
    "format": "prettier . --write",
    "deploy:cf": "wrangler pages deploy ./out --project-name=openmaic-frontend"
  },
